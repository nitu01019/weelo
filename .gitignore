# Exclude build outputs from indexing
build/
.gradle/
*.iml
.idea/libraries/
.idea/caches/
.idea/modules/
.idea/workspace.xml
local.properties
captures/
.externalNativeBuild/
.cxx/

# Node modules
node_modules/
.expo/

# =============================================================================
# SECURITY: Firebase config contains API keys and project identifiers
# Add to .gitignore if repo is PUBLIC. For private repos, it's generally safe.
# =============================================================================
app/google-services.json

# Signing keystores
*.keystore
*.jks

# Android build outputs
app/build/
android_kotlin/
