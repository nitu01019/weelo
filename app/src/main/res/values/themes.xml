<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- ALWAYS LIGHT THEME - No dark mode support -->
    <style name="Theme.Weelo" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/weelo_orange</item>
        <item name="colorPrimaryVariant">@color/weelo_orange</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        
        <!-- Fix black screen during transitions -->
        <item name="android:windowBackground">@color/white</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowDisablePreview">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
    </style>
    
    <!-- Custom window animations to prevent black screen -->
    <style name="WindowAnimationTransition">
        <item name="android:windowEnterAnimation">@android:anim/slide_in_left</item>
        <item name="android:windowExitAnimation">@android:anim/slide_out_right</item>
    </style>
    
    <!-- OTP Input Style -->
    <style name="OtpInputStyle">
        <item name="android:background">@drawable/bg_otp_input</item>
        <item name="android:gravity">center</item>
        <item name="android:inputType">number</item>
        <item name="android:maxLength">1</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Splash Screen Theme - INSTANT load with image -->
    <style name="Theme.Weelo.Splash" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Splash 1 (WEELO logo) shows INSTANTLY as window background -->
        <!-- Uses splash_logo.png - golden yellow background with WEELO logo -->
        <item name="android:windowBackground">@drawable/splash_logo</item>
        <item name="android:statusBarColor">@color/splash_gold</item>
        <item name="android:navigationBarColor">@color/splash_gold</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
    
    <!-- Transparent Dialog Theme for SearchingVehiclesDialog -->
    <style name="Theme.Weelo.Dialog.Transparent" parent="Theme.MaterialComponents.Light.Dialog">
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.6</item>
        <item name="android:windowAnimationStyle">@style/DialogAnimation</item>
    </style>
    
    <!-- Dialog Animation -->
    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>
    
    <!-- Bottom Sheet Dialog Theme - Always attached to bottom -->
    <style name="BottomSheetDialogTheme" parent="Theme.MaterialComponents.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyle</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowSoftInputMode">adjustResize</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.5</item>
    </style>
    
    <!-- Bottom Sheet Style - Forces bottom sheet to stay at bottom -->
    <style name="BottomSheetStyle" parent="Widget.MaterialComponents.BottomSheet.Modal">
        <item name="android:background">@drawable/bottom_sheet_rounded_bg</item>
        <item name="behavior_peekHeight">auto</item>
        <item name="behavior_fitToContents">false</item>
        <item name="behavior_skipCollapsed">true</item>
        <item name="behavior_hideable">true</item>
        <item name="shapeAppearance">@style/BottomSheetShapeAppearance</item>
    </style>
    
    <!-- Shape appearance for rounded corners -->
    <style name="BottomSheetShapeAppearance" parent="ShapeAppearance.MaterialComponents.LargeComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">24dp</item>
        <item name="cornerSizeTopRight">24dp</item>
        <item name="cornerSizeBottomLeft">0dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
    </style>

    <!-- Circular image (driver profile photos, avatars) -->
    <style name="CircleImageView" parent="ShapeAppearance.MaterialComponents.SmallComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
</resources>
