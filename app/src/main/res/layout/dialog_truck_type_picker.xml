<?xml version="1.0" encoding="utf-8"?>
<!--
    Truck Type Picker Dialog - Rapido Style
    ========================================
    Multi-step bottom sheet dialog with horizontal slide transitions
    
    Step 1: Truck Type Grid (with search)
    Step 2: Subtype Selection (with quantity)
    
    Features:
    - Rapido-style clean rounded corners (no drag handle)
    - Horizontal slide animations between steps
    - Search functionality
    - Grid layout for truck types
    - Inline quantity selectors
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/bottom_sheet_rounded_bg">

    <!-- Drag Handle Indicator - Rapido style -->
    <View
        android:layout_width="40dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="12dp"
        android:background="@drawable/bg_drag_handle" />

    <!-- ViewFlipper for step transitions (horizontal slide) -->
    <ViewFlipper
        android:id="@+id/dialogViewFlipper"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:inAnimation="@anim/slide_in_from_right"
        android:outAnimation="@anim/slide_out_to_left">

        <!-- ==================== STEP 1: Truck Type Selection ==================== -->
        <LinearLayout
            android:id="@+id/step1TruckTypeSelection"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <!-- Header - Rapido Style -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingHorizontal="20dp"
                android:paddingTop="20dp"
                android:paddingBottom="8dp">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Add Truck Type"
                    android:textSize="18sp"
                    android:textStyle="bold"
                    android:textColor="#1A1A1A" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Select a truck type to add to your booking"
                    android:textSize="13sp"
                    android:textColor="#666666"
                    android:layout_marginTop="4dp" />

            </LinearLayout>

            <!-- Dotted Divider like Rapido -->
            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:layout_marginHorizontal="20dp"
                android:background="@drawable/dotted_line" />

            <!-- Search Bar -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:layout_marginHorizontal="16dp"
                android:layout_marginTop="16dp"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:paddingHorizontal="14dp"
                android:background="@drawable/bg_search_field">

                <ImageView
                    android:layout_width="20dp"
                    android:layout_height="20dp"
                    android:src="@drawable/ic_search"
                    app:tint="#9E9E9E" />

                <EditText
                    android:id="@+id/searchTruckType"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="1"
                    android:layout_marginStart="10dp"
                    android:background="@null"
                    android:hint="Search truck type..."
                    android:textColorHint="#BDBDBD"
                    android:textSize="14sp"
                    android:textColor="#212121"
                    android:inputType="text"
                    android:imeOptions="actionSearch"
                    android:maxLines="1" />

                <ImageView
                    android:id="@+id/clearSearchButton"
                    android:layout_width="20dp"
                    android:layout_height="20dp"
                    android:src="@drawable/ic_close"
                    android:visibility="gone"
                    app:tint="#9E9E9E"
                    android:background="?attr/selectableItemBackgroundBorderless" />

            </LinearLayout>

            <!-- Truck Types Grid - Scrollable -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/truckTypesGridRecyclerView"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:layout_marginTop="12dp"
                android:paddingHorizontal="12dp"
                android:paddingBottom="16dp"
                android:clipToPadding="false"
                android:overScrollMode="ifContentScrolls" />

        </LinearLayout>

        <!-- ==================== STEP 2: Subtype Selection ==================== -->
        <LinearLayout
            android:id="@+id/step2SubtypeSelection"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <!-- Header with Back Button -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:paddingHorizontal="16dp"
                android:paddingTop="16dp"
                android:paddingBottom="8dp">

                <ImageView
                    android:id="@+id/backToStep1Button"
                    android:layout_width="36dp"
                    android:layout_height="36dp"
                    android:src="@drawable/ic_arrow_back"
                    android:padding="6dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Back" />

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:layout_marginStart="8dp"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/selectedTruckTypeName"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Container Trucks"
                        android:textSize="18sp"
                        android:textStyle="bold"
                        android:textColor="#1A1A1A" />

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Select size &amp; quantity"
                        android:textSize="13sp"
                        android:textColor="#666666"
                        android:layout_marginTop="2dp" />

                </LinearLayout>

                <ImageView
                    android:id="@+id/closeDialogButton"
                    android:layout_width="36dp"
                    android:layout_height="36dp"
                    android:src="@drawable/ic_close"
                    android:padding="8dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Close" />

            </LinearLayout>

            <!-- Dotted Divider -->
            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:layout_marginHorizontal="20dp"
                android:background="@drawable/dotted_line" />

            <!-- Subtypes List with Quantity - Scrollable -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/subtypesRecyclerView"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:layout_marginTop="8dp"
                android:paddingHorizontal="16dp"
                android:paddingBottom="8dp"
                android:clipToPadding="false"
                android:overScrollMode="ifContentScrolls" />

            <!-- Bottom Action Section -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingHorizontal="16dp"
                android:paddingTop="12dp"
                android:paddingBottom="20dp"
                android:background="#FAFAFA">

                <!-- Selection Summary -->
                <LinearLayout
                    android:id="@+id/selectionSummaryLayout"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:visibility="gone"
                    android:layout_marginBottom="12dp">

                    <TextView
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:text="Selected"
                        android:textSize="14sp"
                        android:textColor="#666666" />

                    <TextView
                        android:id="@+id/selectedCountText"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="3 trucks"
                        android:textSize="14sp"
                        android:textStyle="bold"
                        android:textColor="#FF9800" />

                </LinearLayout>

                <!-- Add to Booking Button -->
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/addToBookingButton"
                    android:layout_width="match_parent"
                    android:layout_height="50dp"
                    android:text="Add to Booking"
                    android:textSize="15sp"
                    android:textColor="#FFFFFF"
                    android:textAllCaps="false"
                    app:backgroundTint="#FF9800"
                    app:cornerRadius="8dp"
                    app:elevation="0dp" />

            </LinearLayout>

        </LinearLayout>

    </ViewFlipper>

</LinearLayout>
